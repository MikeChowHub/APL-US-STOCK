{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "APL Table Card Manifest",
  "type": "object",
  "required": ["SchemaVersion", "ScanDate", "Cards"],
  "additionalProperties": false,
  "properties": {
    "SchemaVersion": {
      "type": "string",
      "const": "APL Table Card Manifest v1.0"
    },
    "ScanDate": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "Cards": {
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "items": {
        "type": "object",
        "required": ["CardType", "InputPath", "OutputName", "Required"],
        "additionalProperties": false,
        "properties": {
          "CardType": {
            "type": "string",
            "enum": ["ExecutiveSummary", "TopLeaders", "TopGainers", "SectorStructure", "MarketObservation", "Comparison"]
          },
          "InputPath": {
            "type": "string",
            "minLength": 1
          },
          "OutputName": {
            "type": "string",
            "pattern": "^[^\\\\/:*?\"<>]+\\.png$"
          },
          "Required": {
            "type": "boolean"
          }
        }
      }
    }
  }
}
